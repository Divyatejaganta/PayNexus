apiVersion: v1
kind: Service
metadata:
  name: {{ include "paynexus.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "paynexus.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
